apiVersion: v1
kind: Service
metadata:
  name: rest-server
  namespace: backup
spec:
  type: NodePort
  selector:
    app: rest-server
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30800
